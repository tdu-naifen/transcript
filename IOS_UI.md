# iOS UI — 会议采集、检索与 Mac 协作

> 更新：2026-09-05
> 状态：基于本轮讨论与用户认可的交互 demo 整理的设计规格，不代表正式 App 已实现或通过验收。
> 配套：[Mac UI](MAC_UI.md)、[工程计划](PLAN.md)、[问题清单](BUGS.md)。

## 2026-09-06 用户截图修订

- 保留蓝色主色。以 IMG_1460 / IMG_1461 的录音信息卡、转写区域和圆角悬浮控制为布局参考；不复制未实现的翻译或 AI Analysis 按钮。
- 以 IMG_1461 / IMG_1463 的 Insights 时间轴为参考：动物身份名称、稳定颜色圆点、发言占比、统一时间刻度和播放位置。名字可以全局修改，时间轴点击定位音频，纵向滑动仍用于滚动页面。
- 详情页仅保留原生返回入口。Insights 无论是否已有参与者都可打开；无身份时明确解释状态，不隐藏整个入口。
- 录音按钮拖动和点击互斥，松手有短距离惯性与弹簧回弹；遵循“减少动态效果”。展开与收缩不阻挡 tab 操作。
- 会议图标支持用户自选单个 emoji，持久化到会议记录；动物身份仍使用颜色圆点，而非以会议 emoji 代替身份。
- 文字转写使用 Apple Speech；首次 Apple 语言资源准备不得阻止音频保存。设置不再要求 Nemotron 下载。Apple Speech 不提供声纹身份，这两条能力需分别验收。
- 用户本次明确要求本机自动建立新声音的动物身份，并在跨会议匹配成功时复用；这替代旧的本机逐人手动登记要求，但不等同于授权跨设备发送声纹。该后端接入仍待完成，界面通过不代表它已经完成。
- IMG_1462 对应的 Live Activity 保持现有方向，不在本次布局调整中重做。

## 1. 本轮确定的变化

本文件是本轮 iOS UI 的设计基线。以下决定替代旧 `UI.md` / `PLAN.md` 中对应描述；旧文档暂未同步修改。

| 旧设计 | 本轮设计 |
|---|---|
| 删除 Home，只有录音和设置两个 tab | **Home / Meetings / Settings** 三个 tab |
| 录音按钮固定在右下角 | 独立于 tab bar 的**可拖动浮动录音按钮** |
| 粉色 / 珊瑚色主视觉 | **深蓝主色**；speaker 使用不同颜色的色点 |
| Mac 只分析，不重新转写或调整 speaker | Mac 接收原始录音，重新转写、识别 speaker，并回传结果 |
| 原始识别结果仅由 iPhone 写入 | 转写与 speaker 修改支持双向同步，按操作合并、同字段最后更新生效 |
| 用户确认数据永不被自动处理改变 | 用户要求统一采用最后更新生效；模型操作的生成时点和过期结果处理仍需工程规格明确 |

原始音频仍是核实依据，不因重新处理或编辑而改写。新旧识别版本与编辑操作必须可追溯，不能把同步变成不可恢复的覆盖。

## 2. 导航与页面职责

### 2.1 顶层导航

底部固定三个 tab，顺序为：

1. **Home**：搜索、筛选、连接 Mac、查看进行中的任务。
2. **Meetings**：按时间浏览全部会议。
3. **Settings**：界面语言、录音质量、资源和存储管理。

录音按钮不是第四个 tab。各 tab 保留各自的浏览状态。

### 2.2 会议详情的导航

- 从 Home 搜索结果或 Meetings 列表进入详情。
- 详情隐藏三个 tab，底部改为会议操作区。
- 保留左上角返回按钮与系统左缘右滑返回。
- 返回原入口，保留搜索条件或列表位置。
- Mac 提交确认页单独接管上滑手势，不与返回或录音按钮拖动混用。

## 3. 视觉规则

- 主色：用户提供的深蓝色参考；demo 暂用 **`#06229E`**，是近似值，不是从原图精确采样的色值。
- 主按钮、选中导航、Mac 提交背景使用深蓝。
- 内容背景以白色和低对比中性色为主。
- speaker 色点使用不同颜色，跟随稳定 speaker ID，而非当前排列次序。
- demo 的 Alex 紫色、Otter 金色仅作示例，不限制真实用户身份。
- speaker 名字、时间戳和正文使用中性色，不把整行染成主色。
- 未连接用红点，已连接用绿点；始终同时显示文字，不能只靠颜色表达状态。
- 正式 App 需验证浅色 / 深色、动态字体、VoiceOver、长标题与中英混排；网页的浅色预览不替代这些验收。

## 4. Home

### 4.1 页面组成

自上而下：

1. Home 标题与右上角紧凑的 **Connection** 入口。
2. 一个搜索框：**搜索会议标题或内容**。
3. Speaker 与日期范围筛选。
4. 有任务时显示简洁的 Mac 任务状态。
5. 搜索结果；没有搜索条件时可展示少量最近会议，不复制完整资料库。

### 4.2 搜索

- 默认同时搜索会议标题与已经保存的转写文本。
- Speaker 筛选使用稳定 speaker ID，显示其当前持久化名字和色点。
- 日期范围按会议开始日期筛选，包含起止日期当天；开始日期晚于结束日期时明确提示。
- 搜索、speaker、日期范围可以组合。
- 只选 speaker：查找该 speaker 参与的会议。
- speaker 加关键词：正文匹配限定为该 speaker 的发言；标题命中标记为“标题匹配”，不暗示该词由该 speaker 说出。
- 结果以会议为单位组织，显示标题、日期、时长及匹配片段。
- 点击标题进入会议；点击匹配片段进入详情并定位到对应音频时间。
- 空结果提供清除 / 修改筛选入口，不显示为加载失败。
- 返回搜索页后保留输入、筛选与滚动位置。

这是本地文本检索，不要求 Mac 在线，也不等于生成式问答。Mac RAG 的处理与界面见 `MAC_UI.md`。

### 4.3 Connection 状态

| 状态 | 展示 | 点击行为 |
|---|---|---|
| 未配对 | 红点 + Connect to Mac | 开始首次设置 |
| 已配对、离线 | 红点 + Mac 名称 + Offline | 连接详情与重试 |
| 连接中 | 进度指示 + Connecting | 查看连接进度 |
| 已验证身份且连接可用 | 绿点 + Mac 名称 + Connected | 查看连接详情 / 取消配对 |
| 权限或身份验证失败 | 红点 + 原因说明 | 修复权限或重新配对 |

发现同名设备、存在历史同步时间戳，都不能作为已连接的依据。连接成功与 Mac 模型就绪分开表达。

### 4.4 首次配对

1. 用户在 Mac 打开独立 Transcript App。
2. iPhone Home → Connection → 查找 Mac。
3. 解释本地网络用途；正式 App 的权限由系统请求，不能用自制弹窗冒充授权。
4. 列出发现的 Mac；选择目标设备。
5. 两端显示绑定当前配对过程的相同短码，并分别确认。
6. 完成可信连接后，**自动退出设置并返回 Home**；状态变绿，可短暂显示已连接提示。
7. 不停留在需要再次点 Done 的 Connected 成功页。

失败留在当前步骤并支持重试或返回。取消配对后的重连必须重新确认身份。后台自动重连不应把用户正在浏览的页面强行切到 Home。

权限、设备发现和加密身份绑定的工程方案须按 Apple API 实际能力实现。六位短码是本产品交互，不是 Apple 提供的一套现成 iPhone–Mac 配对 UI。

## 5. Meetings

- 按开始时间倒序。
- 分组：Today、Yesterday、Earlier；历史量增加时按月份细分。
- 每项显示标题、日期 / 时间、时长、参与者色点与适量名字。
- 同步或处理状态是次级信息，不盖过标题。
- 点击进入详情；保留删除和刷新能力。
- 删除涉及本地音频、Mac 副本及同步操作的范围，需另定删除语义，不能从一个列表删除手势直接推定所有副本都应删除。

## 6. 全局录音

### 6.1 可拖动入口

- 深蓝圆形按钮，默认位于右下方、tab bar 上方。
- 在 Home / Meetings / Settings 上独立悬浮。
- 点击开始录音；拖动只移动位置，松手不得误触发录音。
- 可在安全交互区域内自由摆放，松手保留位置，**不强制吸边**。
- 不允许拖出屏幕、压到刘海或 Home indicator；位置需适应屏幕尺寸变化。
- 跨 tab 与重启后保留位置，Settings 提供恢复默认位置。
- 自由摆放可能遮住内容；用户可移动按钮，不承诺任意位置都完全无覆盖。
- 详情及 Mac 提交确认页隐藏该入口，避免遮挡播放器或抢占手势。
- 技术上可使用 SwiftUI `DragGesture`，无需第三方拖动库。

### 6.2 正在录音

- 录音属于全局会话，不随页面销毁。
- 展开态显示计时、电平、实时转写、speaker、暂停 / 停止和收起入口。
- 保留原实时转写新行在上方的布局；历史回放正序。
- 收起为 mini bar，切 tab 后继续录音；mini bar 可重新展开或停止。
- 正在录音时不另显示可启动第二场录音的浮动按钮。
- 录音中 speaker 名字只读，会后才能修改。
- 模型准备不能阻止原始音频可靠保存；未知 speaker 不使整场录音失败。
- 停止采集即结束录音计时和 Live Activity；识别收尾单独显示处理状态。
- 停止后命名，默认日期时间；音频保存不依赖用户完成命名。

## 7. 会议详情

### 7.1 内容区

- 标题、日期时间、时长。
- 参与者与稳定的彩色 speaker 标记。
- 按时间正序显示转写。
- 每段保留音频时间戳，点击定位并播放原始音频。
- 支持修改标题、speaker 名字、某段转写及片段 speaker 归属。
- speaker 名字是全局实体属性，改名影响引用同一 ID 的会议；修改一段归属只改变该片段关联。
- Unknown 保留，不通过强行分配身份消除未知。
- 工程信息和次要操作收进更多菜单。

### 7.2 底部操作区

播放器在上，两个操作按钮在下：

| 按钮 | 行为 |
|---|---|
| Reprocessing | iPhone 从原始音频重新处理；有确认、进度、取消、失败与重试 |
| Process by Mac | 打开深蓝背景的上滑确认页；Mac 未连接时禁用 |

执行中的任务显示“已提交 Mac”或可查看进度的等价状态，不能重复提交同一进行中任务。完成后再次处理应是明确的新任务，不把网络重试当成重新处理。

正文和播放器之间保留足够空间，末行必须完整可见且可点击。

## 8. Process by Mac：确认、发送与异步处理

### 8.1 确认页视觉

参照用户提供的 Robinhood 三阶段截图：

- 深蓝背景铺满屏幕。
- 白色确认卡从上方占据大部分屏幕，底部露出蓝色区域。
- 显示会议、目标 Mac、音频传输状态和本次处理范围。
- 底部提示 **Swipe up to Submit**。
- 上滑时整张白卡跟手移动，距离不足则回弹。
- 提供返回与无障碍提交操作，不强迫所有人使用拖动。

### 8.2 提交后立即释放页面

- 上滑后**立即回到会议详情，发送在后台继续**。
- 不展示必须关闭的阻塞式回执页，不要求用户等待 Mac 接收或处理完成。
- 用户可以返回、搜索、切 tab、浏览其他会议或继续使用录音功能。
- 用非阻塞提示与任务状态显示发送进展。
- “发送中”“Mac 已接收”“处理中”“结果已同步”必须区分。
- 只有收到 Mac 的持久化接收回执，才能显示“Mac 已接收”。

### 8.3 断开、失败和重试

- 上滑时尚未连通：不提交，提供返回或重连。
- 发送过程中断：保留待发送任务并提示未送达；重连后续传 / 重试。
- 同一任务重试沿用同一 ID，不在 Mac 生成重复任务。
- Mac 已接收后，iPhone 离线不取消 Mac 任务。
- 离线时显示最后已知进度和离线说明，不伪造实时完成状态。
- Mac 完成后保存结果，重连后回传。
- Mac 处理失败不破坏原始录音、已有转写或其他会议。

网页仅模拟后台任务；正式 iOS App 被挂起后的传输续行能力要单独实现与验证，不能用“用户可以离开页面”推导出进程被挂起后仍能无限运行。

## 9. 两端编辑与统一版本

本轮确定使用 **operation-style 修改 + last update wins**。

- iPhone 用户编辑、Mac 用户编辑、Mac 模型结果，都记录为有来源的操作。
- 同一实体的同一字段发生冲突：按统一确定的操作顺序，最后更新生效。
- 不同字段的修改分别保留，不用一端整份会议覆盖另一端。
- 操作有唯一 ID，重复接收不重复执行；断线、乱序、重传后仍应收敛。
- 编辑本地先保存，离线显示待同步；重连后两端得到同一有效版本。
- 展示本地已保存、待同步、已同步、同步失败；必要时可查看最近更新来自哪一端。
- demo 的操作记录是验收辅助，不要求正式首页一直展示技术日志。

正式实现不能直接拿两台设备的裸墙钟作唯一排序依据。逻辑时钟 / HLC、并列排序、模型长任务的基准版本及延迟回传如何排序，需在后续工程 planning 中明确。

Mac 重新转写可能改变分段，片段编辑不能仅锚定临时 segment ID。时间范围、原文快照与新段映射的规则须先定；无法可靠映射时显示待确认，而不是静默丢弃修改。

## 10. iOS 验收清单

- [ ] Home / Meetings / Settings 的顺序、职责与返回路径一致。
- [ ] 标题 / 内容 / speaker / 日期组合搜索正确，日期结束日包含当天。
- [ ] 搜索命中片段能跳转音频时间，返回保留搜索条件。
- [ ] 浮动录音键可拖动、持久化、不越界、不误触录音，可恢复位置。
- [ ] 录音收起、切页、再展开保持同一会话；停止与收尾状态准确。
- [ ] 配对分别等待两端确认，成功自动返回 Home；失败不伪装已连接。
- [ ] Home 红 / 绿点同时有文字；权限、连接与模型就绪不混为一谈。
- [ ] 详情隐藏 tab，显示播放器与两个处理按钮；末行不被遮挡。
- [ ] 上滑不足回弹；提交后立即恢复浏览，不出现阻塞式回执页。
- [ ] 发送中断可恢复；重试不重复任务；Mac 已接收后可脱离手机处理。
- [ ] Mac 的转写与 speaker 更新回到 iPhone；iPhone 编辑反向同步。
- [ ] 离线同字段冲突按确定顺序收敛，不同字段修改不丢失。
- [ ] 原音频不被覆盖；失败 / 取消保留已有结果。
- [ ] 本地化、VoiceOver、动态字体、浅深色和长文本回归。

## 11. 当前证据与后续工作

- 用户已认可本轮 demo 的总体交互方向，并要求写两端 UI Markdown。
- 本轮异步 demo 已做浏览器测试：配对返回 Home、上滑后自由导航、Mac 离线处理、双端转写与 speaker 编辑、同字段双向 LWW、重连收敛、录音拖动与 mini bar。
- 这些测试只证明模拟交互，不证明真实网络、模型准确率、数据库可靠性或 SwiftUI 行为。
- iOS 功能验收继续遵守仅 Simulator 的现有范围。Apple 明确指出 Simulator 不支持本地网络隐私机制，该权限分支须标记未验证，不用 mock 代替。
- 当前 demo：`.superpowers/brainstorm/18066-1788628090/content/async-sync-v5.html`。
- 后续工程 planning 与旧文档统一更新另行进行；本文件不包含提交代码或变更数据库的授权。

### IOS-COMPLETE-FIX-03 实施验证（2026-09-05）

- AppServices 共享音频所有权；RecorderModel 在采集启动前同步撤销播放器并释放播放会话，启动中、采集中及收尾期间禁止旧详情激活或停用录音会话。
- Home 分页失败可用同一 cursor 重试；成功页去重，过期成功/失败不改新筛选。实际 SearchRepository 集成覆盖 33 场会议、标题/说话人正文分型及日期筛选。
- 中英切换使用所选语言资源 bundle，导航标题、tab 和控件即时更新，不重建导航状态；System 标签跟随系统语言。已删除搜索不可用的过时页脚。
- 最终专用 Simulator 组合验证：38 个 App hosted tests（含原 14 个录音回归）及 9 个 fixture UI tests 全部通过。覆盖一次定位/重载、25 小时秋季 DST、浮动位置跨 tab/重启/旋转/复位、实际开关筛选、分页返回及末段完整可见并可播放。
- 最终证据：`.build/ios-fix03-final3.log`、`.build/ios-fix03-final3.xcresult`、`.build/ios-fix03-final3-attachments/`。音频为隔离生成的 WAV/AAC，UI 数据为 DEBUG 显式启用的内存 fixture；不是麦克风/模型声学 E2E。
- 隔离更正：收紧 fixture 启用条件后，旧转写测试曾意外回落到专用 Simulator 磁盘库，其故障注入污染了该库；重复验证在 `ios-fix03-final2` 暴露此问题。本轮已改为显式注入内存库，并隔离 hosted App 启动服务；最终回归通过。此前磁盘库未修补或删除，不能将该 Simulator 的正常启动视为已验收；需要另行授权恢复后再做非 fixture QA。
- Mac 真实传输、持久回执、双端同步及本地网络隐私分支仍未验收；生产入口继续显示未配置服务原因，不伪造连接。以上为实现者验证，不代表独立 review/QA 通过，也不关闭本规格其余验收项。

### IOS-ISOLATION-FIX-04 测试存储约定

- DEBUG 测试启动必须同时提供 `TRANSCRIPT_TEST_STORAGE=1` 与 UUID 格式的 `TRANSCRIPT_TEST_RUN_ID`；仅设置样例开关、缺少/错误 run ID 或试图传入路径都会拒绝启动，不回落到用户库。
- `TRANSCRIPT_UI_FIXTURE=1/0` 只选择是否播种样例。两种模式都使用 `Library/Caches/TranscriptTestRuns/<UUID>/Transcript/` 下的磁盘数据库和 Audio；同 run 重启保留数据，不同 run 隔离。没有任何测试选项时仍使用正常 Application Support 持久存储；Release 不启用测试存储。
- UI 测试统一通过 `isolatedApp` 使用共享的 `TestStorageConfiguration.launchEnvironment`，两条真实录音入口也不例外。保留真实采集/模型实现；launch-config 测试不启动它们，不代表真实麦克风/模型 E2E 已验收。
- hosted 测试 scheme 从 build setting 展开 UUID，每次命令须附加 `TRANSCRIPT_TEST_RUN_ID="$(uuidgen)"`；同一次重启验证重复使用同一个 UUID。UI 测试另生成各自 UUID，作为 xcresult 附件保留。测试结束后先关闭服务/进程，只清理本次记录的 UUID 根，不能扫描删除其他 run 或生产数据。
- 存储针对性验证：44 hosted（含原 14 个录音回归）+ 1 real-flow launch-config 测试通过；新的安全设备 `A565D0B8-502B-4BA0-9704-E6D4845065BA` 的正常 DB/WAL 字节哈希、大小、mtime 与音频元数据均未改变。旧污染设备保持不动。
