# iOS 当前产品范围

更新：2026-09-07。本文件是要求，不是完成声明；状态统一见 [验收表](SYNC_ACCEPTANCE.md)，工程/测试见 [README](README.md)。
新反馈：**Identifying 耗时、App 前台保持唤醒、底部录音控件/上滑动画仍在本轮修复范围，尚未验收**。

## 导航与视觉

- Home / Meetings / Settings 三个 tab 各保留浏览状态。Home 提供标题/正文搜索、speaker 和日期组合筛选、连接入口；Meetings 按时间浏览资料库。录音不是第四个 tab。
- 搜索使用稳定 speaker ID 和当前名字；speaker + 关键词的正文命中限该人的发言，标题命中单独标识。结束日期包含当天，非法范围提示；片段命中可定位原音频，返回保留筛选/滚动位置。
- 详情隐藏 tab，保留原生返回及左缘返回手势；底部播放器/处理操作不得遮挡最后一段文字。整个录音会话不随页面销毁。
- 深蓝品牌 `#06229E`，深色交互前景使用可读浅蓝，正文/元数据用系统中性色。speaker 色点按稳定身份跨会议保持一致，连接状态同时显示文字，不能只靠红绿。
- 浅/深色、增强对比度、VoiceOver、动态字体、长名字和中英混排需要原生回归；交互目标至少 44pt。尊重“减少动态效果”，不把网页 demo 当作原生证据。

## 采集、收起与生命周期

- 点击即可靠采集；模型准备不阻塞计时、电平、暂停/停止或音频保存。展开显示实时转写（新行在上）、speaker 和录音控制；收起为可恢复的 mini bar，切 tab 不停止采集。
- 未录音时没有空底部附件。浮动入口拖动与点击互斥，不越安全区；按较新的截图要求松手短暂 ease-out 吸附最近左右边缘，无弹跳/形变。位置跨 tab/重启保存并可复位；详情与提交页隐藏该入口。
- 收起/展开、底部控件和上滑提交互不抢手势；长文本末行可滚入完整可读区域。动画实际流畅度仍需测量，不用查询微基准代表帧率。
- **App 在前台使用期间保持唤醒**是本轮新要求；离开前台应恢复系统行为，不新增后台执行承诺或改变用户系统设置。
- 停止采集即停计时和 Live Activity；识别收尾另示处理状态。旧 Activity/异步结果必须绑定原 meeting ID，不能停止或污染下一场录音；覆盖暂停、锁屏停止、失败、重启清理。
- 音频封存后即允许命名/播放，日期时间预填且确认名非空；弹窗中断不丢录音，不做 LLM 自动命名。已移交独立协调器的旧会话收尾不能阻止下一场采集。
- AAC/fMP4 原字节封存不可改写；采集帧数/采样率是正常录音时长依据，不用包含暂停的墙钟或 AAC padding 延长时长。数据库原子发布文件名、哈希、字节数和时长；恢复失败保留原文件并报错。
- 后台指 App 内并发不等于 iOS 挂起后持续推理；非活跃时的声纹/推理任务保留可恢复状态。实时说话人处理不启用后台 GPU。

## 转写语言与重新处理

- Apple SpeechAnalyzer/SpeechTranscriber 负责转写，Sortformer/CAM++ 独立负责说话人。Apple Speech 不提供声纹身份；不以旧 Nemotron 安装状态表示 Apple 资源就绪，不静默转云端/旧引擎。
- 界面语言紧凑菜单仅“跟随系统 / 简体中文 / English”，即时生效并持久化。系统选项标签按设备语言而非 App 选择；与录音语言独立。
- 设置中录音语言仅是未来会议默认值，System 不表示自动检测。当前会议可请求语言切换：准备期间继续原录音/转写，就绪后在音频边界切换，不重写此前文字。
- 展示实际语言、待切换语言、仅录音、下载/加载/取消/错误/重试。取消准备不保证系统共享下载立即停止，不支持/拒绝权限不得无限重试。
- 沿用全 App 两个 Apple speech engine 租约、有限 PCM 队列及可恢复溢出策略；超时/清理未完不能提前复用原生租约。音频保存独立于这些资源。
- 无转写也可从原音频 Reprocess；确认、进度、取消、失败和重试可见。采用录音语言而非后改默认值，多语言整文件替换不支持时明确拒绝并保留原文。
- 提交前核验实际音频哈希、完整输入/修订快照；成功原子替换，过期/失败/取消不得覆盖新编辑。AAC 解码尾部只按已封存采集范围处理，不能截去真正越界结果来掩盖错误。
- 归档质量与引擎输入格式解耦，保留语音/高保真选择；重采样遵循各引擎要求，不改原音频。

## 身份、详情与删除

- 每行有音频时间戳和身份标签，无法判断就显示本地化 Unknown/待识别；未归属不能使整场保存失败，真实采集/存储失败必须明确提示。
- 录音模型持续观察持久身份/名称/颜色，收起视图或没有新 ASR 文本也能更新。实时、详情、搜索、重启后引用同一稳定实体，不把模型槽位数当参与人数。
- 足够可靠的新声音在本机建立动物身份，跨会议可靠匹配时复用；当前名与最初动物名保留。可靠样本不足、重叠或混合段不强行归属；名字本身不是匹配成功证据。
- 模板按实际模型指纹与预处理命名空间隔离，沿用匹配策略。Identifying 的采样等待、模型推理、数据库提交、UI 观察延迟分开测；不能靠降低阈值制造更早标签。
- 录音中名字只读；会后参与者、文本名字及 `⋯ → Speaker Insights` 使用同一全局改名流程，失败保留输入并支持重试。Insights 无身份时也可打开并解释状态，不伪造可命名对象。
- 发言时长来自 diarization 区间，不是字数或 ASR 段跨度。区分单人、重叠、未知语音、缺失分析和静音；分母、合并、舍入规则须用标注音频核实，不能隐藏未知后归一化。若逐人含重叠应说明总和可能超过 100%；未就绪不是 0%。
- 详情正序转写，支持播放/暂停、±5 秒、拖动定位、倍速与点段落播放。手动滚动暂停自动跟随而非音频，“跟随播放”或点段落恢复。标题、emoji、文字、片段归属可编辑；全局改名与单段归属是不同操作。
- 左滑/长按/详情菜单删除均先确认，取消不提前移除。保护正在采集/保存/处理的目标会议，不锁死其他会议。会议删除清理会议及其音频/转写/关联/派生结果，但保留全局身份、名字、声纹；跨端结果按 [v1 契约](SYNC_CONTRACT.md)，不能仅凭本地消失声称全部设备已删。

## 与 Mac 协作

- 配对按 [冻结协议](MAC_PAIRING_PROTOCOL.md)：校验身份后显示六位 SAS，两端分别确认；成功自动回 Home，失败/拒绝/取消不假装成功。已配对重连严格校验 pin，身份不匹配不能降级新配对；后台断开保留信任。
- Connection 区分未配对、离线、连接中、已认证、权限/身份错误。自动重连不抢用户页面；配对、自动同步、声纹分享分别授权。
- 已启用的新同步应自动收敛数据/音频，详情可查看状态。Mac 重处理结果必须回到既有 iPhone 会议并触发现有页面刷新；**用户报告该结果回传尚不正常**。
- 保留的上滑提交交互使用深蓝底与整张白卡跟手移动，未达距离回弹；返回和无障碍提交可用。提交后立即返回详情，不等待传输完成，用户能继续浏览。
- 旧 immutable-copy v2 只能明确表示 Send/Retry 副本，不得标为已处理或已回传。收到持久回执才算 Mac 已接收；100% 字节进度不是提交完成。
- 发送重试复用 operation ID；处理中、离线最后状态、结果已同步分开显示。不凭 App 内导航自由承诺进程挂起后无限传输。离线编辑/重分段/过期发布遵循 [自动同步契约](SYNC_CONTRACT.md)，不使用旧“仅 iPhone 可编辑”规则。
