# Mac 当前产品范围

更新：2026-09-07。入口与测试见 [README](README.md)，是否通过见 [验收表](SYNC_ACCEPTANCE.md)。
唯一客户端为 [MacApp](MacApp)，根 [project.yml](project.yml) 的 `TranscriptMac`；原生 SwiftUI/AppKit，不是网页、Catalyst 或另一个 `mac/` 工程。

## 当前状态与本轮缺口

- 基线 `3bfc72b` 包含自动同步、会议内 ASR 处理、集中模型设置及全局声纹资料展示/改名。
- **Mac 重处理未回到 iPhone、Mac 本地生成/识别声纹缺失、会议整卡点击区域**是 17:42 用户反馈后的活动修复，尚未验收。
- 基线 `MacCoreProcessingRunner` 是 ASR-only。共享 CAM++ 在 macOS/iOS Simulator 真模型测试通过，不等于 Mac 产品任务已调用声纹模型；正在进行的修复必须另附产品路径证据。
- LLM/RAG、自动摘要和分析界面不是本轮交付要求。既有用户 LLM 数据、配置、模型与保留资料不因范围收缩而删除；不把旧 Analysis 功能列为本轮成功项。

## 窗口、阅读与编辑

- Meetings、Voiceprints 为当前核心导航，连接由右上角原生面板承载，不恢复旧重复 Connection/Processing 侧栏；模型管理全部进入原生 Settings。
- 基线还含 LLM Analysis 入口，其本轮隐藏/整理不等于删除用户数据；具体代码变更须独立验证，不由文档预宣告完成。
- 会议列表/阅读区可调宽度，保留原生搜索、键盘选择和焦点。**整张会议卡包括留白均应可选中**，内嵌按钮/菜单保留独立语义，不能只有文字可点。
- 与 iOS 共用深蓝 `#06229E` 品牌及稳定身份色点；深色交互前景采用可读浅蓝，不让系统强调色覆盖品牌。正文中性色、播放器固定但不挡末行，连接点同时带文字。
- 详情包含标题、来源/日期/时长、参与者、正序转写和原音频播放器；点时间戳可定位。离线仍可读取已保存资料，新结果更新同会议而非重复副本。
- 标题、转写、全局 speaker 名字及单段归属的编辑按 [同步契约](SYNC_CONTRACT.md) 发布。全局身份用稳定 ID，不用名字匹配；merge/split 不等同改名，不能擅自扩展操作语义。
- 处理中保留已发布结果；远端更新不能清空草稿、抢焦点或强制换页。区分本地保存、待同步、失败及同步完成。
- 中英文、浅/深色、增强对比度、长标题、缩放、键盘和 VoiceOver 需原生验收；[网页预览](design/mac-app.html) 是历史示意，不证明网络、音频或模型执行。

## 会议处理与模型设置

- 真实会议内提供 Process、实际阶段/进度、取消、错误/重试、已完成版本和需确认的 Reprocess；只读示例不得处理，不显示计时器伪造的进度。
- 输入为已验证原音频，任务保存音频 revision、完整输入 token、处理配置和来源。自动同步的已授权音频可靠采用后可入队；旧 v2 副本回执、仅元数据、资源描述符和 `audioVerifiedOnMacAt` 都不授予该权限。
- 可靠音频提交到入队之间须可崩溃恢复；重复标题更新、重连、重复收件不得创建重复任务。收到任务不等于模型就绪；缺少配置显示等待，不静默下载。
- Settings 集中 ASR、diarization、speaker embedding 的目录、兼容性、安装确认、逐模型进度/校验、取消、错误和重试。保留原有模型和用户选择；下载失败不得删除已验证版本，不执行任意模型卡代码。
- 关闭窗口不取消任务；睡眠/退出/崩溃不声称持续推理。启动将中断任务转为需重试；失败/取消不被重连隐式重跑，完成任务换配置不暗中重处理。
- 完成发布时重新核验会议仍存在、实际输入和修订未变；分段映射、编辑冲突、过期结果与幂等发布遵循 v1 契约。失败/取消保留原音频、标题和旧结果。
- **本轮需要 Mac 本地 diarization/CAM++ 生成与匹配、可靠发布以及 iPhone 可见回传闭环。** 展示声纹页或安装成功不能代替这些操作；身份不确定仍保留 Unknown。

## 全局声纹与数据保留

- Voiceprints 从全局仓库加载，不由现存会议反推：显示当前名、最初动物名、实际模板模型/预处理、维度、样本数及更新时间。改名影响同 ID 的历史引用。
- 向量图只展示真实数据，不是可播放波形或可解释的音高；缺失、旧来源、损坏、非有限值分别提示，不用随机向量补空。
- 不同模型/预处理向量不混比；真实文件指纹和可靠非重叠语音决定模板可用性。未确认的归属不能循环扩充长期模板，阈值不得为过测试而放宽。
- 会议删除清除会议、音频、转写、会议关联及派生分析/索引；保留全局身份/最初名/用户名字和声纹。不得秘密保留已删会议音频提供“声音样本”。
- 独立身份删除、解除配对、暂停/撤销生物特征分享各有不同边界。每个已固定 peer 的显式同意才允许相应交换；撤销不是既有全局资料/审计历史的远端抹除，详见 [ADR](SYNC_ADR.md)。
- 已有本地 M4A 导入保持源文件不变，复制/解码/哈希验证后才发布。只读示例不落用户库、不带假音频；切换会议应卸载前一音频。
- 备份使用一致 SQLite 快照与引用音频校验；工作数据库留在本地，选定目录由 security-scoped bookmark 授权。保留 v1 校验与 v2 完成 ASR 候选的兼容性，不备份配对密钥、模型权重、bookmark 或未完成任务。
- 备份可能含录音、文字、身份/声纹且不额外加密；历史备份不会随当前删除自动清理，本地完成不等于 iCloud 上传完成。“Verify Snapshot”只是校验，不恢复或改库。

## 连接与兼容性

- [配对协议](MAC_PAIRING_PROTOCOL.md) 和 [immutable-copy v2](MAC_SYNC_PROTOCOL.md) 冻结不变：committed-ECDH、六位 SAS 双端确认、持久 pin、同一加密通道；不是 TLS，也不能放松信任检查。
- 新配对需用户开启限时窗口；恢复发现不重新允许陌生设备配对。同名不等于同身份，忘记设备先撤销本机信任，不删除会议。
- 本地网络许可与防火墙传入许可是两层；只解释原因/恢复方式，不自动批准权限、不关闭防火墙。用户已报告拔 USB 后可用；真实路径诊断和重连证据仍须另记。
- 发现发布、已配对和在线分开。休眠清理会话，唤醒只恢复用户启用的发现，重新认证后才显示在线；手动停止不得被自动恢复覆盖。心跳失效不是瞬时双端断网通知。
- 旧副本接收默认关闭，启用后由 iPhone 显式 Send/Retry；开关变化使旧会话失效，重连重新协商。新自动同步为独立能力与权限，不从旧 copy 推断授权。
- v2 保留 768-byte raw chunk、4096-byte 加密外帧和持久回执；Mac 本地上限为 128 MiB 音频、16 MiB 转写、4 个接收任务、256 MiB 预留，总量超限在 status 前 `failed/storage`。未完成 staging 七天无活动过期，不清已提交回执或取消绑定。
- 音频/转写/哈希/事务完成才确认，100% 仅表示字节进展。重复回执、重启、prefix 续传、碰撞、删除 tombstone、错误哈希和旧端兼容继续回归，完整选择见 [验收表](SYNC_ACCEPTANCE.md)。
