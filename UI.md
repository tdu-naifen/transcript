# iPhone UI 设计

> 配套 [PLAN.md](PLAN.md)。这里只写 iPhone 采集端的界面决策，不重复产品定位和数据模型。
> 最后更新：2026-09-05
> 本轮新增迁移和修复要求均待实施、待验收；历史布局描述不是当前功能通过验收的证明。

---

## 0. 三条原则

**录音是全局状态，不是一个页面。** 90 分钟的会议中途，用户会想翻上一场记录、会想改个设置。录音不能因为离开某一屏就中断，也不能因此看不见。

**读内容是一等公民。** 每一屏的首要问题是「这场会说了什么」。命名、参数、同步状态都是二等，让路。

**音频是 ground truth。** 文本会错（PLAN §1：ASR 对数字最易出错）。每一行 transcript 都必须能一点就跳到对应音频。

---

## 1. 导航

```
┌──────────────────┐
│ 录音              │
│ ▸ 周会    32:11  │
│ ▸ 1:1 Alex 18:02 │
│ ▸ 客户电话 45:30 │
│                  │
├──────────────────┤
│ ☰ 录音   ⚙ 设置 ⏺│
└──────────────────┘
```

**两个 tab + 一个常驻录音键。**

`Home` 被删除。它原本唯一的内容是录音，而录音已提升为全局浮层；剩下的只能是「最近录音」——那就是 `录音` tab 本身。**录音列表就是首页。**

录音键 `⏺` 不属于任何 tab，浮在 tab bar 右侧，任何位置一点即录。

---

## 2. 录音（全局浮层）

录音是覆盖在任意 tab 之上的浮层，可收起为 mini bar，参照 Apple Music 的 now-playing。

### 2.1 展开态

```
┌────────────────────────────┐
│  ⌄                  02:38  │ ← 下拉收起 · 计时
│ ─────────────────────────  │
│                            │
│ ● Marmot · 02:29           │
│ Los mamoncillos me         │
│ encantan también because   │
│ they're like a mezcla…     │
│                            │
│ ● Puffin · 02:19           │
│ Oh, okay, Maria.           │
│                            │
│ ● Marmot · 02:14           │
│ Okay, um anybody else?     │
│                            │
│ ⋯⋯⋯ 被控件遮挡并淡出 ⋯⋯⋯  │
│         ⏸    ⏹            │
└────────────────────────────┘
```

**新行在顶部。** 与直觉相反但正确：播放控件浮在底部，新行若长在底部会被自己的控件盖住。倒序下被遮挡淡出的永远是最旧一条。

**顶部只有计时。** 原先的 `M4A` / `16 kHz` / `2240 ms` / `Ready` 四个 chip 全部删除：

- 三个是编码参数，与 Settings 的 Capture section 逐字重复
- 录音中它们都改不了，显示 = 噪声
- 语言 chip 也删；界面语言与识别语言独立，不暗示 Apple ASR 已支持自动语言检测

**不显示固定 chunk 延迟。** 灰色的 partial 行表达尚未定稿的结果（覆盖 PLAN `1h`）。Apple 转写迁移后不再显示 Nemotron 品牌或固定 2240ms 参数。

**动物名只读。** 点击给轻提示「录完可以改名」。理由见 §4.2。

### 2.2 收起态（mini bar）

```
├────────────────────────────┤
│ ● 02:38  ▁▃▅▂▆▃▁      ⏹  │
├────────────────────────────┤
│  ☰ 录音      ⚙ 设置    ⏺ │
└────────────────────────────┘
```

计时 + 电平 + 停止。

**放电平而不是「谁在说话」。** 用户在别的 tab 里真正的焦虑是「它还在录吗」，不是「现在谁在说」。电平连续变化，不像名字会跳字分散注意力。

### 2.3 准备、停止与命名

- 点击录音后立即开始采集；Apple 转写或说话人模型准备中只影响对应处理状态，不能让用户误以为音频尚未保存。计时、电平与暂停/停止操作保持可用。
- 停止后立即停止录音计时和录音指示；若推理仍在收尾，显示处理状态而不是仍在录音，不清空已有 transcript。
- 弹出录音命名窗口，默认是本地日期与时间，确认时名称不能为空。音频必须先可靠保存，弹窗被中断也不能丢失录音。详情页仍可编辑标题。
- 暂不实现 Apple 本地 LLM 自动命名。
- 无法分辨 speaker 时显示本地化的“未知说话人 / Unknown speaker”，保留文本、时间戳和播放入口。不得弹出 `incompleteSpeakerAssignments(count: ...)` 把整场录音判作失败；真正的采集/存储错误要明确提示。

### 2.4 Live Activity 与锁屏

锁屏、Dynamic Island、mini bar 与展开页共享采集状态。暂停不继续计时；从 App 或锁屏停止都走同一停止流程，采集结束就终止录音 Activity，不能等待 ASR/声纹处理完成才停止显示 still recording。

处理失败也必须结束录音 Activity；防止较晚到达的更新覆盖结束状态，启动时清理不对应真实录音的遗留 Activity。Simulator 中分别验证 App 内停止、锁屏停止、后台/失败收尾与重启。

---

## 3. 会议详情

```
┌────────────────────────────┐
│ ‹  周会              ⋯     │
│ ●●● 3 人 · 44:30           │ ← 点开展开参与者
│                            │
│ ● Alex · 00:17             │
│ But that is not so simple. │
│                            │
│ ● Marmot · 00:19           │
│ Of course, data centers    │
│ need staff.                │
│                            │
│ ● Alex · 00:22             │
│ According to the           │
│ municipality, they…        │
│                            │
│ ╭────────────────────────╮ │
│ │ ▂▄█▆▃▁▂▄▆█▃▁▂          │ │
│ │ 00:17          44:30   │ │
│ │  ⟲5    ▶    ⟳5   1x   │ │
│ ╰────────────────────────╯ │
└────────────────────────────┘
```

**transcript 正序**（00:17 → 00:19 → 00:22），与录音中的倒序相反 —— 回放时跟着音频走。

**点任意一行跳到该时间点。** 这是 PLAN §1「3 秒内 verify」的落点；验收播放、前后 5 秒跳转、拖动定位和倍速，不仅检查控件存在。

**参与者折进标题行。** 命名是一次性动作（命名一次，所有会议生效），不值得每次进来都占首屏。点 `●●● 3 人` 展开：

```
│ 参与者                      │
│ ● Alex    42%      ✓       │
│ ● Marmot  31%   [命名]     │
│ ● Puffin  27%   [命名]     │
```

点 transcript 里的动物名同样可以改名，两条路径等价。

**`⋯ → Speaker Insights` 必须提供可发现的改名入口**，与参与者列表和文本行使用同一 speaker 实体。仅录音结束后在 iPhone 上改名；历史会议同步显示新名字。

每条 transcript 都有 speaker 标签（含未知）和音频时间戳，实时页与详情页的归属一致。离开重进与 App 重启不得丢文本或标签。详情菜单提供重新处理：即使没有 transcript 也可从原音频恢复，显示进度、取消、错误及重试；失败/取消不清掉旧结果。

**工程信息收进 `⋯` → 详细信息。** SHA-256、文件大小、"On disk: Yes"、同步时间戳 —— 保留但不占首屏。

---

## 4. Speaker

### 4.1 命名模型

```
新声纹 ──▶ 随机分配一个未使用的动物名 ──▶ 用户随时改 ──▶ 所有历史会议同步更新
```

数据层已就绪：`Speaker.id` 跨会议稳定，`displayName` 一改，所有引用该 id 的地方全变（`resolvedName`）。UI 只需显示 `resolvedName`。

**不使用 "Speaker 1 / 2 / 3"。** `MeetingSpeaker.displayIndex` 退化为仅供 `colorIndex` 使用。

**随机而非顺序取名。** 当前 `AnonymousNameGenerator.nextName` 返回池中第一个空位，导致第一场会议永远是 Hippo、Otter、Badger。改为随机：不同会议之间名字不撞脸，也避免用户误以为「Hippo 总是第一个说话的人」。

### 4.2 录音中不可改名

录音中 speaker 名只读。理由：实时聚类会改判，用户此刻写下的名字随时可能被推翻，而用户会认为那是他确认过的。

**录完再命名，用户确认过的身份不被后续自动处理推翻。** 录音结束不代表模型归属一定正确；不确定段仍保留未知。

这条决策顺带消除了整类冲突 —— 录音中不存在「用户写的名字被机器推翻」。

### 4.3 命名即锁定

跨会议自动识别仍可能出错（会议 #5 靠声纹认成 Alex，离线重聚类发现认错）。此时：

```
用户命名过的 speaker，不再被自动拆分。
聚类发现新人时，只把新的那些行分出去给新动物名，
已归属的行一字不动。
```

```
03:00  用户把 Marmot 改成 Alex
  ● Alex · 01:20  你好
  ● Alex · 02:40  对

10:00  聚类发现里面混了另一个人
  ● Alex   · 01:20  你好      ← 不动
  ● Alex   · 02:40  对        ← 不动
  ● Puffin · 05:10  那个…     ← 新分出
```

用户看到的是「又多了一个人」，不是「我写的名字被改了」。**用户写过的名字永不被机器推翻。**

冲突发生时用户不在场，标记冲突等他下次打开详情页处理即可。

### 4.4 颜色

每个 speaker 一个稳定色点（`Speaker.colorIndex`），跨会议一致。名字可以改，颜色不变 —— 用户对「蓝色那个人」的肌肉记忆比对名字更快。

保留现有粉色/珊瑚色录音背景与材料风格，但需在 Simulator 浅色、深色外观下检查文字、图标、卡片底色与材料对比度，排查硬编码颜色与系统动态色混用。修复控件遮挡和卡片裁切；不把弹窗系统蒙层变暗直接当作颜色故障。

### 4.5 发言占比与不确定性

- 发言时长来自音频 diarization 时间线，不是文字长度或整段 ASR 的起止跨度；UI 显示名字不代表已验证真实身份。
- 明确区分单人发言、多人重叠、未确定时段与静音。不能隐藏未知部分、把已归属部分重新归一化后伪装为全场准确占比。
- **展示口径待实现前确认**：建议主分布使用互斥的单人、重叠、未确定语音时段，以总语音时长为分母，静音单独统计；逐人统计若含重叠，应明确允许总和超过 100%。未确定是否有语音的缺失分析时段不能当作静音。
- 无有效时长或尚未分析时显示未就绪而不是假 0%；示意图的 42%/31%/27% 只代表无重叠且全部归属的示例。

分母、区间合并、舍入和零时长规则需与 PLAN §8.1 的标注时间线测试一致。

---

## 5. Settings

### 5.1 录音质量

归档质量与 ASR 输入解耦：

```
麦克风 44.1 kHz stereo
   ├──▶ 归档 M4A（质量可选）
  ├──▶ 格式转换 ──▶ Apple SpeechAnalyzer
  └──▶ 16 kHz mono ──▶ 说话人模型
```

```
录音质量   [语音 ▾]
   语音    16 kHz mono     1×
   高保真  44.1 kHz stereo ~4×
分析输入格式由各引擎适配，不作为用户设置
```

**唯一真实理由：两年后换更好的模型可以重新转写。** 16 kHz 归档就永远只能到这个上限。代价是存储约 4×。

Apple ASR 输入采用其支持/推荐的格式；说话人模型保留所需的 16 kHz mono。移除 Nemotron 专属固定格式说明，不改变用户选择的归档质量。

### 5.2 界面语言

用系统下拉菜单替代展开排列的选项；收起时显示当前选择，展开时标识已选项。仅有三个选项：跟随系统、简体中文、English。

- “简体中文”和“English”始终用各自语言命名。
- 系统语言为中文时，系统选项显示“跟随系统”；系统语言为英文时显示“Follow System”，其他系统语言暂按英文回退。
- 系统选项标签基于当前设备系统首选语言，不随 App 内手动选择的语言改变；不能误用 App 的 resolved locale。
- 保持选择持久化、切换即时生效，默认跟随系统。这是界面语言，不自动改变录音识别语言。
- Simulator 覆盖系统英文/App 中文、系统中文/App 英文、选择系统与重启保留；只出现三个选项。

### 5.3 模型与设备信息

分别呈现 Apple 转写语言资源与 Sortformer / CAM++ 的就绪、下载、取消、失败和重试状态；不能以旧 Nemotron 下载完成代表新转写可用。不在录音主界面展示模型工程参数。

设备信息保留。Capture section 的三个只读参数删除；归档质量在 §5.1 表达。

---

## 6. 历史布局任务与本轮验收

下表保留原布局任务，不表示这些功能仍未实现；应先核对当前代码，已有功能只做回归，不重复重写。本轮以 §6.3 为验收清单。

| 改动 | 涉及 |
|---|---|
| 删除 Home tab，录音列表成为首页 | `RootView` |
| 录音提升为全局浮层 + mini bar | 新增，`RecordView` 重构 |
| 录音页删除 4 个 chip + 3 个 picker | `StatusChipRow`（可整个删除）、`RecordView` |
| transcript 行加 speaker 色点 + 名字 | `LiveTranscriptView.TranscriptLineView` |
| 🔴 详情页加音频播放器 | `MeetingDetailView`，全新 |
| 详情页 transcript 改正序 + 点击跳转 | `MeetingDetailView` |
| 详情页工程信息收进 `⋯` | `MeetingDetailView` |
| 参与者折进标题行 + 命名入口 | `MeetingDetailView`，全新 |
| 动物名改为随机分配 | `AnonymousNameGenerator.nextName` |
| Settings 加录音质量选择 | `SettingsView`、`AudioFileWriter` |
| 列表页加 `.onDelete` | `RecordingsListView`（并解决 PLAN §9.4.1 音频文件生命周期） |

### 6.1 先决条件

**DEBUG fixture 必须先做。** 上述所有布局判断都建立在假想文字上 —— 中英混排、名字长短不一的真实 transcript 到底挤不挤，ASCII 图证明不了。

沿用 `RootView.swift` 已有的 `SmokeRecording` 模式：

```
-uiFixture 1    灌入若干场假会议 + 带 speaker 的假 transcript
```

`#if DEBUG` 包住，不进 Release。

Fixture 仅用于布局验证，不能证明模型加载、真实麦克风、说话人准确率或数据持久化通过。

### 6.2 迭代顺序

1. **DEBUG fixture** —— 能反复看到真实密度
2. **录音列表（新首页）** —— 最简单，验证色点 / 动物名 / 时长这套视觉语言
3. **会议详情** —— 播放器是产品核心缺口
4. **录音浮层** —— 改动最大，留到视觉语言已确立

每步：改 → build → 模拟器截图 → 一起看 → 调整。

### 6.3 本轮验收顺序（仅 Simulator，待执行）

1. Apple 资源可用性与真实转写；模型准备期间音频仍采集，双人语音的标签/时间戳可见。
2. 停止、日期时间预填命名、Live Activity 结束；未知归属不阻止保存，不显示原始错误枚举。
3. Speaker Insights 改名、发言占比口径、离开重进与终止重启的数据一致性。
4. 播放/定位与重新处理的成功、取消和重试；保护原音频与用户确认数据。
5. 语言下拉菜单、系统语言交叉组合、浅色/深色截图、长文本/滚动和底部控件遮挡。
6. 功能通过后清理确认无用的代码，再回归相关测试、App/widget build 和上述交互；按逻辑改动独立提交。

不安排真机测试；Simulator 的截图或性能结果不代表真机表现。不支持的 Apple API、锁屏交互或音频路由应记录为未验证/阻塞，不能用 fixture 冒充验收。

---

## 7. 明确不做

- **Record / Translate 分段控件** —— 参考 app 有翻译能力，本项目没有；更换 ASR 不扩大到翻译功能
- **transcript 第二行译文** —— 同上
- **compute unit / ANE 标签** —— 工程诊断信息，不属于用户界面
- **录音中显示 chunk 延迟数值** —— partial 行已经表达了
