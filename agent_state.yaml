performance agent:
  purpose: "优化 TranscriptCore 数据库搜索与异步声纹接口；不修改 UI。"
  status: "implementing"
  constraints: "主 agent 不写代码；子 agent 独占文件；所有成果经独立审查与 Simulator 验收；不提交、不清理用户数据。"
  plan: "/Users/tingzhen/.claude/plans/iterative-wishing-lovelace.md"
  steps:
    - id: "explore-design"
      status: "complete"
      result: "3 个只读探索与 1 个设计子 agent 完成；实施计划已批准，尚未运行测试。"
    - id: "db-search"
      status: "dispatching"
      purpose: "安全迁移、全文搜索、日期/speaker 筛选与分页。"
    - id: "voiceprint-candidates"
      status: "dispatching"
      purpose: "缓存候选、失效机制、匹配策略及独立入库接口。"
    - id: "voiceprint-worker"
      status: "dispatching"
      purpose: "有界后台 CAM++ 服务、采样、模型复用与取消。"
    - id: "simulator-harness"
      status: "dispatching"
      purpose: "独立后端 Simulator 测试入口，不改 App/UI。"
    - id: "integration-verification"
      status: "pending"
      purpose: "重新处理集成、测试、基准与独立审查；未通过不得宣称完成。"
  report: "尚未实现完成或验收。"

local_agent_takeover:
  updated_at: "2026-09-05"
  work_root: "/Users/tingzhen/Desktop/transcript.worktrees/local-agent-functionality"
  branch: "agents/local-agent-functionality"
  baseline_sha: "d0fedeb4069ec71fd9ff74bb801de3ad7ac58577"
  integration_target: "main; blocked until main WIP ownership is confirmed"
  status: blocked
  state_owner: orchestrator
  historical_state: "The inherited performance section above is not current execution or acceptance evidence."
  dispatch:
    structure: "Orchestrator coordinates leaf implementation, review and QA workers; it does not directly edit product code."
    selected_worker_model: "gpt-5.6-luna"
    routing: "Actual runtime route not independently observable."
    permissions: "Prompt boundaries only; no tool-enforced read-only restriction claimed."
    owners:
      - {task: BUG002-FIX-02, worker: unknown-speaker-writer, status: implemented, files: none, result: "Stopped writing; shared real drain path, actual repository failure tests, and probe compile fix. Reports five hosted tests passed."}
      - {task: BUG002-REVIEW-03, worker: unknown-speaker-reviewer, status: implemented, files: none, result: "All previous local findings closed; no new high-confidence runtime defect. Acoustic E2E remains unverified."}
      - {task: "BUG002-QA-02 / APPLE-PROBE-02", worker: recording-qa, status: implemented, files: none, result: "Independent five hosted tests passed; Apple runtime unavailable. Device released."}
      - {task: BUG007-FIX-01, worker: activity-writer, status: implemented, files: "RecorderModel.swift, RecordingActivityController.swift, narrow helper/protocol, RecordingSession.swift, and AppTests/RecordingActivityLifecycleTests.swift", excludes: "TranscriptionModel.swift, existing BUG002 tests, project.yml, agent_state.yaml, main", result: "Final implementation stopped capture before awaiting Activity.end; binds end to its Activity; adds once orphan snapshot, nonreentrant stop/failure publication, stale end/update guards, and session drainInProgress protection."}
      - {task: BUG007-REVIEW-01, worker: activity-reviewer, status: implemented, files: none, result: "FINAL and REENTRANCY reviews closed all findings; local lifecycle review passed."}
      - {task: BUG007-QA-REENTRANCY, worker: recording-qa, status: implemented, files: none, result: "Independent final double-suite QA passed exit 0: RecordingActivityLifecycleTests 9/9 and TranscriptionModelBUG002Tests 5/5, 14/14. Device closed; report and evidence retained."}
      - {task: REC-READ-01, worker: recording-gap-reader, status: implemented, files: none, result: "Read-only trace found BUG-002/007 and missing Apple production runner; no tests executed."}
  handoff:
    main_sha: "d0fedeb4069ec71fd9ff74bb801de3ad7ac58577"
    main_wip: "UI, search, voiceprint, reprocessing, Sync contract and mac/ remain untouched in main."
    requests: ["HANDOFF-LOCAL-01 to iOS controller", "HANDOFF-LOCAL-02 to performance controller"]
    result: "Product writers for this local round have stopped; recording QA is only validating and writing its own session report. Main WIP ownership/stop is still not confirmed."
    user_confirmation: "User unavailable; main integration remains blocked, not implicitly authorized."
    processes: "No current evidence of an external writer; prior restore incident is attributed to unknown-speaker-writer and was recovered. Main stop remains unconfirmed."
    unblock_owner: "User or old controllers must confirm stopped main writers and artifact ownership."
  incidents:
    - "unknown-speaker-writer mistakenly ran git restore -- agent_state.yaml and removed orchestrator coverage. Writer admitted action and stopped. No HEAD change. Orchestrator restored its own state; worker instructed never to restore unowned changes."
  resources:
    simulator:
      id: "02F43A35-8005-4485-86CE-54EACA41D857"
      name: "Transcript-local-agent-6751623e"
      runtime: "iOS 26.4.1 (23E254a)"
      owner: recording-qa
      state: "Session-created, booted, simctl getenv and Mobile MCP verified responsive; final QA completed and device closed."
      scope: "BUG007 hosted lifecycle tests; no existing Simulator touched, no microphone/model execution."
    derived_data: ".build/bug007-handoff-derived"
    release_condition: "Satisfied: final QA report/evidence retained; DerivedData retained for handoff; no other device touched."
  requirements:
    - id: BUG-001
      source: "BUGS.md:17-24; PLAN.md:94-121"
      implementation: "RecordingSession, LiveRecordingDrain, TranscriptionModel"
      owner: orchestrator
      dependencies: ["BUG-002", "REQ-004", "Simulator acoustic input"]
      status: blocked
      acceptance: "Stop/name/reopen/restart preserve text, timestamps, identities and recoverable source audio."
      evidence: "Drain/seal/recovery tests exist; no current real App restart E2E."
      next: "Fix stop defects, then verify short/preparation/tail persistence."
    - id: BUG-002
      source: "BUGS.md:26-32"
      implementation: "App/ViewModels/TranscriptionModel.swift finish and transcript validation"
      owner: orchestrator
      dependencies: ["review fixes", "independent QA", "main integration"]
      status: blocked
      acceptance: "Unassigned speaker remains Unknown and saveable; real drain/storage failures still fail."
      evidence: "Independent review closed all local findings; independent five hosted tests passed. Old-gate mutation rejected unknown/mixed."
      next: "User/platform must unblock Apple Simulator acoustic acceptance; old controllers/user must release main WIP for integration. No acceptance commit or done before required gates."
    - id: BUG-003
      source: "BUGS.md:34-41"
      implementation: "Diarization timeline and ASR alignment"
      owner: orchestrator
      dependencies: ["REQ-004", "annotated two-speaker audio", "exclusive model runtime"]
      status: blocked
      acceptance: "Annotated turn boundaries/attribution agree without forced identities or relaxed thresholds."
      evidence: "No current acoustic verification."
      next: "Verify after persistence and ASR readiness."
    - id: BUG-004
      source: "BUGS.md:43-50"
      implementation: "Speaker repository and live/detail projections"
      owner: orchestrator
      dependencies: ["BUG-001", "identity policy"]
      status: blocked
      acceptance: "Stable entities across live/detail/restart; global names propagate without fabricated identities."
      evidence: "No current integrated runtime verification."
      next: "Check saved identity projections after stop fixes."
    - id: BUG-005
      source: "BUGS.md:52-59; UI.md:212-219"
      implementation: "SpeakerTalkShare.swift currently sums ASR utterance spans"
      owner: orchestrator
      dependencies: ["user decision on denominator/overlap/unknown/missing-analysis display"]
      status: blocked
      acceptance: "Audio-interval shares distinguish unknown, missing analysis, overlap and silence."
      evidence: "UI.md explicitly says display semantics require confirmation; current tests encode old zero/ASR-span rules."
      next: "User to confirm display/denominator rules before changing calculations."
    - id: BUG-006
      source: "BUGS.md:61-69"
      implementation: "AudioCaptureEngine, TranscriptionModel, LiveTranscriber"
      owner: orchestrator
      dependencies: ["REQ-004", "lossless catch-up design", "same-input performance baseline"]
      status: blocked
      acceptance: "Immediate reliable capture and catch-up; preparation-stop preserves audio; measured phase timings."
      evidence: "Reader found unbounded chunks while models load; no measurement or approved latency threshold."
      next: "Design bounded lossless catch-up without silently dropping audio; measure only in reserved runtime."
    - id: BUG-007
      source: "BUGS.md:71-78"
      implementation: "RecorderModel.swift:167-198; RecordingActivityController.swift"
      owner: activity-writer
      dependencies: ["BUG-002 writer finished", "lifecycle test", "supported lock-screen interaction"]
      status: blocked
      acceptance: "Capture stop ends Activity before inference drain; late updates cannot reactivate it."
      evidence: "Local writer log .build/bug007-reentrancy-final.log and xcresult exit 0 with 14/14; independent activity-reviewer FINAL and REENTRANCY reviews closed all findings; independent BUG007-QA-REENTRANCY report qa-activity-reentrancy.txt independently passed exit 0 with lifecycle 9/9 plus BUG002 5/5. Source hashes in that report match the current tested files; no mutation residue remains."
      next: "User/environment/old controller must provide supported Simulator acoustic/ActivityKit lock-screen evidence and main handoff; then run target-branch integration and obtain a local acceptance commit. Do not mark done now."
    - id: BUG-008
      source: "BUGS.md:80-86; approved iOS plan"
      implementation: "Main App UI WIP"
      owner: orchestrator
      dependencies: ["UI writer handoff", "Simulator"]
      status: blocked
      acceptance: "Readable list/settings/detail/recording/dialogs and status bar in both appearances."
      evidence: "Main UI changes not integrated."
      next: "User/old controller to confirm handoff, then appearance QA."
    - id: BUG-009
      source: "BUGS.md:88-94; approved iOS plan"
      implementation: "Main navigation/recording/player WIP"
      owner: orchestrator
      dependencies: ["UI writer handoff", "Simulator"]
      status: blocked
      acceptance: "Collapse/navigate/reopen same recording; final transcript unobscured and seekable."
      evidence: "Main UI changes not integrated."
      next: "User/old controller to confirm handoff; do not duplicate WIP."
    - id: REQ-001
      source: "BUGS.md:102"
      implementation: "RootView.swift naming flow"
      owner: orchestrator
      dependencies: ["BUG-001", "Simulator restart QA"]
      status: blocked
      acceptance: "Nonempty date-prefilled editable title; audio saved independently of confirmation."
      evidence: "Existing naming UI test; restart/audio-independence not verified."
      next: "Verify after stop fixes."
    - id: REQ-002
      source: "BUGS.md:103; approved performance plan section 4"
      implementation: "MeetingReprocessor and MeetingReprocessingRepository; main integration WIP"
      owner: orchestrator
      dependencies: ["performance handoff", "REQ-004", "Simulator cancel/restart"]
      status: blocked
      acceptance: "Atomic publication; cancel/failure preserve audio, ID, title, names and old results."
      evidence: "Main integration report parse-only; run-scoped physical-completion API missing."
      next: "Old owner/user to release WIP; review cancellation before integrating."
    - id: REQ-003
      source: "BUGS.md:104"
      implementation: "Localization/Settings and main UI WIP"
      owner: orchestrator
      dependencies: ["UI handoff", "Simulator language QA"]
      status: blocked
      acceptance: "System/zh-Hans/en immediate and persistent; independent from ASR language."
      evidence: "No current language/restart verification."
      next: "User/old controller to release UI ownership."
    - id: REQ-004
      source: "BUGS.md:105; main performance contract response"
      implementation: "AppleSpeechBaseline/probe only; production remains Nemotron"
      owner: orchestrator
      dependencies: ["Simulator Apple resource availability", "shared production runner"]
      status: blocked
      acceptance: "Actual Simulator offline en/zh/mixed transcription and timestamps through production."
      evidence: "Independent probe confirms SpeechTranscriber.isAvailable=false, supported=0, installed=[], en/zh status unsupported (exit 65) on iOS 26.4.1. No Apple production runner."
      next: "User/platform must provide a supported Simulator environment; no Mac/native/cloud/old-engine substitution. Re-run probe before production integration."
    - id: REQ-005
      source: "BUGS.md:106"
      implementation: "Model readiness and settings"
      owner: orchestrator
      dependencies: ["REQ-004", "UI handoff"]
      status: blocked
      acceptance: "Independent Apple/Sortformer/CAM++ state and actual download/cancel/failure/retry."
      evidence: "Nemotron availability is not Apple readiness."
      next: "Runner owner establishes resource API before UI readiness claims."
    - id: REQ-006
      source: "BUGS.md:107"
      implementation: "No deletions"
      owner: orchestrator
      dependencies: ["feature acceptance", "main integration"]
      status: blocked
      acceptance: "Proven unused static/dynamic/build references, compatibility preserved, separately tested deletion commit."
      evidence: "No accepted features, deletions or worktree cleanup."
      next: "Wait for acceptance; preserve all user data/worktrees."
    - id: SEARCH
      source: "Approved iterative-wishing-lovelace plan section 1; IOS_UI.md section 4"
      implementation: "Main migration/SearchRepository/HomeModel WIP"
      owner: orchestrator
      dependencies: ["performance/UI handoff", "frozen API", "Simulator"]
      status: blocked
      acceptance: "Preserving migration; literal/date/speaker paging, titleMatched, seek and UI races."
      evidence: "Review/fix reports read; fixes claim parse only, not runtime/performance acceptance."
      next: "Old owners/user release WIP; review/test before wiring."
    - id: VOICEPRINT
      source: "Approved iterative-wishing-lovelace plan sections 2-4"
      implementation: "Main matcher/processor/selector/binder/reprocessing WIP"
      owner: orchestrator
      dependencies: ["performance handoff", "consent ruling", "exclusive CAM++ runtime"]
      status: blocked
      acceptance: "Bounded jobs, stale-match rejection, cancellation and real same/different-speaker tests."
      evidence: "Candidate/worker/harness/integration reports read; no current model tests or baseline."
      next: "Old owner/user release WIP; verify fixes without claiming calibration."
    - id: SYNC-EDIT
      source: "IOS_UI.md section 9; MAC_UI.md section 8; approved Mac/iOS plans"
      implementation: "Main Sync contract WIP"
      owner: orchestrator
      dependencies: ["user scope and deletion-propagation decisions"]
      status: blocked
      acceptance: "Approved editable fields, deterministic offline merge/deduplication and stale-result handling."
      evidence: "UI supports body/assignment editing; Mac plan narrows human edits to names."
      next: "User reconciles scope; do not choose by implementation convenience."
    - id: SYNC-CONSENT
      source: "Main SYNC_CONTRACT.md:25-27; main agent_state.yaml:126-128,158-161"
      implementation: "Main Sync artifact/enrollment APIs"
      owner: orchestrator
      dependencies: ["user consent-policy ruling"]
      status: blocked
      acceptance: "Approved registration/sync policy; names never imply biometric consent."
      evidence: "Both per-speaker confirmation and automatic enrollment are recorded as user decisions."
      next: "User reconciles conflicting decisions; no automatic enrollment here."
    - id: SYNC-TRANSPORT
      source: "IOS_UI.md sections 7-8; approved Mac plan section 2"
      implementation: "Main contract/presentation, no accepted transport"
      owner: orchestrator
      dependencies: ["Mac handoff", "reviewed authentication protocol", "two-end runtime"]
      status: blocked
      acceptance: "Bilateral authenticated pairing, durable outbox/receipts, resume/idempotency and result return."
      evidence: "Contract explicitly excludes networking/storage/pairing/authorization."
      next: "Obtain handoff and reviewed protocol; values are not transport."
    - id: MAC-PIPELINE
      source: "Approved gleaming-brewing-avalanche plan"
      implementation: "Main untracked mac/ foundation and BM25 work"
      owner: orchestrator
      dependencies: ["Mac handoff", "REQ-004", "SYNC-TRANSPORT", "model/resource validation"]
      status: blocked
      acceptance: "Native persistent processing, remote cancel, version publication, BM25/Gemma citations and real return."
      evidence: "Foundation round 1 report claims live 8 unit + 2 UI passes; not rerun against current source."
      next: "Confirm ownership/source version, independently verify foundation/BM25/model integration."
    - id: IOS-INTEGRATION
      source: "Approved streamed-bouncing-music plan phases 2-3"
      implementation: "Main App UI WIP"
      owner: orchestrator
      dependencies: ["UI handoff", "SEARCH", "SYNC-TRANSPORT"]
      status: blocked
      acceptance: "Three tabs with real services, nonblocking submission and localized accessible navigation/state."
      evidence: "Four UI branches in main; no current integrated acceptance."
      next: "User/old controller confirms stop; orchestrator owns end-to-end gate."
  evidence:
    environment: "Xcode 26.6 (17F113); Apple Swift 6.3.3; arm64 macOS 26"
    baseline: "git status --short empty and git diff --check exit 0 before implementation."
    prototype:
      source: ".superpowers/brainstorm/18066-1788628090/content/async-sync-v5.html"
      selection: "Explicit iOS specification/approved-plan reference, not highest version by default."
      observed: "Three tabs; title/text/speaker/inclusive-date filters; iPhone confirmation still waits for Mac."
      screenshot: "/Users/tingzhen/.copilot/session-state/6751623e-5db4-4ec4-a0df-a2ce62737d71/files/prototype-awaits-second-confirmation.png"
      limitation: "Browser simulation only; page disappeared before second confirmation, no real pairing evidence."
    bug002_initial:
      command: "swift test --package-path Packages/TranscriptCore --scratch-path .build/bug002 --filter BUG002UnknownSpeakerTests"
      exit_code: 1
      log: ".build/bug002/BUG002UnknownSpeakerTests.log"
      result: "Dependency safe.bareRepository=explicit prevented SwiftPM's bare checkout access; no compilation or test execution."
      recovery: "Only a single dependency-tool process may override safe.bareRepository for its known dependency checkouts; no persistent Git config changes."
      review: "BUG002-REVIEW-01: two coverage findings returned to writer; existing repository-only tests cannot detect restored App gate."
    bug002_fix_round_1:
      source: "TranscriptionModel.swift, AppTests/TranscriptionModelBUG002Tests.swift, project.yml"
      tests: "unknown-only, mixed assignments, no transcript, drain failure through actual finish()"
      mutation: {exit_code: 65, log: ".build/bug002-xcodebuild-old-gate.log", result: "Old gate rejects unknown/mixed as expected."}
      fixed: {exit_code: 0, log: ".build/bug002-xcodebuild-tests.log", result: "Writer reports four hosted tests passed on session Simulator."}
      limitation: "Not acoustic E2E, restart acceptance or main integration; independent QA/review pending."
    independent_qa_round_1:
      report: "/Users/tingzhen/.copilot/session-state/6751623e-5db4-4ec4-a0df-a2ce62737d71/files/qa-recording.txt"
      bug002: "Four hosted tests passed, exit 0; .build/qa-recording-bug002-only.log and .xcresult."
      apple: "Initial combined build exit 65: await inside XCTUnwrap autoclosure; probe did not run at this stage."
    bug002_fix_round_2:
      fixed: {exit_code: 0, log: ".build/bug002-fix2-bug002-final.log", result: "Writer reports five hosted tests passed."}
      combined: {exit_code: 65, log: ".build/bug002-fix2-xcodebuild.log", result: "BUG002 passed; Apple probe failed runtime availability."}
      apple: {exit_code: 65, log: ".build/bug002-fix2-probe.log", result: "SpeechTranscriber.isAvailable=false on session iOS Simulator."}
      implementation: "Real and injected branch finish operations now share LiveRecordingDrain.wait; repeat finish rejected after cleanup; actual repository append/read errors exercised."
      limitation: "Local review/QA now passed; no acoustic E2E or main integration."
    independent_qa_round_2:
      report: "/Users/tingzhen/.copilot/session-state/6751623e-5db4-4ec4-a0df-a2ce62737d71/files/qa-recording-final.txt"
      bug002: {exit_code: 0, passed: 5, log: ".build/qa-recording-final-bug002.log", result_bundle: ".build/qa-recording-final-bug002.xcresult"}
      apple: {exit_code: 65, log: ".build/qa-recording-final-probe.log", result_bundle: ".build/qa-recording-final-probe.xcresult", result: "isAvailable=false; supported=0; installed=[]; en/zh unsupported"}
      source_git_blobs:
        TranscriptionModel.swift: "d9b9440b30d454d150b0a5fbe91049da1d6c615e"
        TranscriptionModelBUG002Tests.swift: "a41fae3b61657d7efa1fdbcf8bc05f82bb372e14"
        AppleSpeechRuntimeProbeTests.swift: "0dc8b80614dd676b124fd24aab6d445f501690c5"
      review: "BUG002-REVIEW-03 closed prior findings; no new high-confidence runtime bugs."
    bug007_round_1:
      log: ".build/bug007-tests.log"
      result_bundle: ".build/bug007-tests.xcresult"
      result: "Only existing BUG002 five tests passed; no Activity test exists in this snapshot. Not BUG007 acceptance."
      review: "BUG007-REVIEW-01 reported four runtime issues; fixes and targeted lifecycle tests returned to writer."
    historical_reports: "Main performance review/fix/harness/integration reports and mac/docs/foundation-report.md read; not treated as current acceptance."
    final_qa:
      report: "/Users/tingzhen/.copilot/session-state/6751623e-5db4-4ec4-a0df-a2ce62737d71/files/qa-activity-reentrancy.txt"
      result: "Independent double-suite exit 0; RecordingActivityLifecycleTests 9/9 and TranscriptionModelBUG002Tests 5/5, 14/14."
      logs: [".build/bug007-reentrancy-qa.log", ".build/bug007-reentrancy-qa.xcresult"]
      source_hashes_match_report: true
      mutation_residue: "none"
      device: "02F43A35-8005-4485-86CE-54EACA41D857 closed after QA"
      derived_data: ".build/bug007-handoff-derived retained"
      workers: "All product, review and QA workers idle; no other task executing."
      limitation: "Local hosted/lifecycle evidence only; real ActivityKit lock-screen/microphone, main integration and commit gates remain unmet."
  commits: []
  cleanup: []
  done_gate: "Approved + implemented + wired + independently reviewed + required QA + target-branch verified + locally committed. No requirement is done."
