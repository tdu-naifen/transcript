# 本轮问题与回归清单

> 更新：2026-09-05。依据用户截图、复现描述及讨论整理。
> 配套 [PLAN.md](PLAN.md) §8.1 与 [UI.md](UI.md) §6.3；不替代产品决策。
> 本文没有重新执行测试。历史修复报告不等于当前版本通过，以下问题均需复核与验收。

## 状态与验收规则

- **待复核**：用户曾观察到问题，当前版本是否仍存在尚未确认。
- **需求待验收**：已明确的交互或迁移要求，不代表已复现代码缺陷。
- P0：数据保存/恢复风险；P1：核心录音、识别或状态可信度；P2：界面与设置。
- 仅在 Simulator 验收；历史真机截图保留为问题证据，不安排真机测试。
- 模型文件输入测试、fixture 布局测试、真实麦克风 E2E 分开记录。真实麦克风测试应由宿主机扬声器播放双人会议，再经麦克风进入 Simulator，不能直接注入音频冒充。
- 每次验收记录提交号、宿主机/Xcode/runtime、模型或语言资源、输入样本、操作、预期/实际结果、日志或截图。构建通过不能关闭功能问题。
- 根因栏只列排查假设；不以强制分配 speaker、降低匹配阈值或吞掉异常来制造成功。

## BUG-001 停止后重新打开录音，转写为空

**P0 · 待复核**

- 证据：用户录音后停止，回主列表再打开刚录的会议，发现 transcript 消失。
- 复现：录到实时文本出现 → 停止 → 完成命名 → 返回列表 → 重新打开；另测终止并重启 App。
- 待查：推理/事件消费/数据库收尾是否被提前取消，结果是否只留在内存，详情查询与 meeting ID 是否一致。不能仅凭空页面断言数据库已丢失。
- 验收：已生成的文本、时间戳、speaker 归属在重进和重启后保留；短录音、模型准备期间停止、尾段尚未定稿均覆盖。原始音频始终可恢复，处理未完成须显示真实状态。

## BUG-002 未归属 speaker 导致整场录音报错

**P1 · 待复核**

- 证据：停止时弹出 `Recording problem`，内容为 `incompleteSpeakerAssignments(count: 3)`；页面已有四段文本。
- 待查：停止收尾是否把每段必须具有 speaker ID 设成成功条件；部分识别结果与采集/存储失败是否混为一类。
- 验收：无法归属时保留本地化的“未知说话人 / Unknown speaker”、文本、时间戳及音频，允许保存和命名；不展示原始错误枚举。真实存储失败仍明确报错，不能统一忽略。

## BUG-003 多人讲话被归为同一个 speaker

**P1 · 待复核**

- 证据：用户明确听到两个人讲话，实时界面却归成同一个人。
- 待查：Sortformer 输出本身是否正确；是否丢失 finalized 历史；ASR 长段是否跨越换人时刻；对齐是否只选一个最大重叠槽位；暂停/重采样后的时间基准是否一致。
- 验收：以有人工时间标注的双人轮流发言样本核对区间、换人边界和误归属；有可靠词级时间戳时细分跨人文本，证据不足保留未知。
- 边界：同时讲话的活动检测不等于两路独立转写；本轮不承诺对重叠语音分别输出完整文本。声纹相似度也不等于已确认身份。

## BUG-004 参与者列表与文本归属不一致

**P1 · 待复核**

- 证据：详情列出三个参与者，但多数文本仍是 Unknown speaker；另一场历史截图有文本却显示 `0 participants`，无可用改名入口。
- 待查：槽位到稳定 speaker ID 的映射、最终归属落库、实时/详情投影是否一致；是否只创建了参与者而没有足够归属证据。
- 验收：同一最终结果在实时页、详情及重启后使用一致的实体和标签，每行均有 speaker 标签（允许未知）与音频时间戳；不得因列表有三个人就强行把未知文本分给他们。
- 参与者存在时，`⋯ → Speaker Insights` 可修改其名字，历史引用同步更新；没有已识别实体时不伪造声纹或身份来启用改名。

## BUG-005 发言百分比不可信

**P1 · 待复核**

- 证据：1:54 会议显示三个参与者，占比为 29%、0%、0%，同时存在大量 Unknown 文本。截图不足以得出真实比例。
- 待查：是否用 ASR 长段跨度代替实际发言时间，是否把未分析算作静音，是否遗漏未知/重叠，是否错误归一化或舍入。
- 验收：以 diarization 音频区间及人工标注核对，不用文字长度。区间合并、分母、重叠、未知、静音和零时长规则先按 [UI.md](UI.md) §4.5 确认；缺少分析时显示未就绪而非假 0%。
- 无法归属的语音与无法判断是否有语音的缺失分析区间必须区分；若逐人计时包含重叠，明确总和可能超过 100%。

## BUG-006 开始录音后模型准备过久，转写迟迟不出现

**P1 · 待复核**

- 证据：会议已开始，界面仍显示 `Loading model...`、`0 utterances`；截图计时为 00:07。用户同时反馈声纹分析慢，当前缺少分阶段耗时数据。
- 待查：资源安装、ASR 加载/预热、Sortformer 加载/推理、CAM++ 提取、对齐/查库分别耗时多少；是否每次重建模型或重复处理全部历史。
- 验收：点击录音即开始可靠采集，模型准备时计时/电平/停止可用；准备完按原音频时间线追赶，不丢帧、不重复，准备期间停止仍保存音频。
- 分别报告冷启动、热复用、首个 partial、final、积压、内存及各说话人模型耗时；性能目标基于测量确认，不编造通过阈值。
- 已确定迁移 Apple SpeechAnalyzer/SpeechTranscriber，但迁移本身不是性能修复的验收证据，且不能消除独立的 diarization 问题。

## BUG-007 停止录音后锁屏仍显示正在录音

**P1 · 待复核**

- 证据：用户停止 recording 后，锁屏 Live Activity 仍显示 still recording。
- 待查：Activity 是否跟随整个识别任务而非采集状态结束；异步更新是否覆盖结束状态；启动时是否残留旧 Activity。
- 验收：采集停止即结束录音计时和 Activity，不等待 ASR/声纹收尾；覆盖 App 内停止、锁屏停止、暂停、错误收尾和重启清理。晚到结果不能重新激活已结束的录音状态。
- 若 Simulator 不支持某锁屏交互，记录阻塞，不能以代码检查替代实际交互通过。

## BUG-008 浅色背景出现白字，界面不可读

**P2 · 待复核**

- 证据：用户 iPhone 截图中 Recordings 标题、空状态正文和状态栏为白色，背景近白；后续截图正常不代表所有页面/外观已修复。
- 待查：固定浅色背景与系统动态前景色、材料、呈现层级的外观是否混用。
- 验收：Simulator 系统浅色/深色下，列表、设置、详情、录音浮层、命名与 speaker 弹窗均可读，状态栏与背景匹配。保留现有视觉设计，不把正常弹窗蒙层变暗算作故障。

## BUG-009 录音浮层与底部控件布局问题

**P2 · 待复核**

- 证据：用户曾反馈录音页无法退出浏览其他页面；后续截图中转写面板底部呈直线截断，详情底部文本与播放器/导航叠放。
- 待查：浮层生命周期是否绑定采集，safe-area/inset、滚动尾部留白、裁切与渐隐是否符合设计。渐隐本身不一定是 bug，应检查最后一行能否完整滚入可读区域。
- 验收：录音可收起、切 tab、重新展开，同一会话持续采集；播放器、导航、录音控制不互相遮挡；长文本最后一行可完整阅读和点击定位。

## 已确认需求与迁移风险

以下为需求缺口/回归项，不凭讨论判定当前实现必然失败。

| ID | 要求 | 验收要点 |
|---|---|---|
| REQ-001 | 停止后命名 | 日期时间预填，确认名不可为空，可后改标题；音频保存不依赖弹窗确认；不做 LLM 自动命名 |
| REQ-002 | 从音频重新处理 | 无 transcript 也可触发；有进度、取消、重试；成功原子替换结果，失败/取消保留旧结果、原音频、会议 ID、标题及确认过的名字 |
| REQ-003 | 界面语言下拉 | 仅跟随系统、简体中文、English；系统选项标签依据设备系统语言而非 App 所选语言；立即生效且重启保留，与 ASR 语言独立 |
| REQ-004 | Apple ASR 迁移 | Simulator 实际验证 API、语言资源安装、离线转写、中文/英文/混说及时间戳；不支持则报告阻塞，不暗换真机、Mac 原生、云端或旧引擎 |
| REQ-005 | 模型就绪状态 | Apple 语言资源与 Sortformer/CAM++ 分别显示状态；旧 Nemotron 就绪不能代表新转写可用；下载、取消、失败、重试需实际验证 |
| REQ-006 | 最后清理无用代码 | 功能验证后核对调用再删除；保留仍被说话人模型、重处理、widget 和回归测试使用的代码；不删用户数据库、音频、签名或未提交改动 |

## 建议处理顺序与关闭记录

1. 先确认 Simulator 上 Apple ASR 的可用性；并行排查 BUG-001/002 的保存风险和 BUG-007 的采集状态。
2. 统一说话人时间线、身份映射和统计口径，修复 BUG-003/004/005；对 BUG-006 分阶段测量后优化。
3. 回归命名、重新处理、语言菜单、外观和布局，最后清理确认无用的代码并再次验收。

每个逻辑修复单独提交。关闭条目时填写：修复提交、复现输入、执行步骤、实际结果、测试/截图路径与仍未覆盖的条件。当前没有条目在本文中标记为已关闭。